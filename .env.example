# Local dev (SQLite)
DATABASE_URI=file:./habitat.db

# Production (Vercel Postgres / Neon) — set POSTGRES_URL to use PostgreSQL
# POSTGRES_URL=postgres://user:pass@host/db?sslmode=require

PAYLOAD_SECRET=your-secret-here-change-in-prod
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
